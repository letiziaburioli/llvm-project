# Autogenerated from /home/letizia/llvm-project/llvm/utils/lit/tests/lit.site.cfg.in
# Do not edit!

# Allow generated file to be relocatable.
from pathlib import Path
def path(p):
    if not p: return ''
    return str((Path(__file__).parent / p).resolve())


import sys

config.lit_tools_dir = ""
config.llvm_src_root = "/home/letizia/llvm-project/llvm"
config.llvm_obj_root = "/home/letizia/llvm-project"
config.llvm_tools_dir = lit_config.substitute("/home/letizia/llvm-project/./bin")

import lit.llvm
lit.llvm.initialize(lit_config, config)

# Let the main config do the real work.
lit_config.load_config(config, "/home/letizia/llvm-project/utils/lit/tests/lit.cfg")
